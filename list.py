class List():
    def __init__(self, head):
        self.head = None
        self.next = None
    
    def insert(node):
        if self.head = None:
            self.head = node #genesis node
        if self.head.next = None: 
            self.head.next = node #sneaky
            
    def delete():
        pass

    def search(self, value, next_node): #use recursion
        if next_node.data != value:
            self.search(value, next.next)
        if self.head.data != value:
            self.search(value, self.head.next)
        